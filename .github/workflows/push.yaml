name: push
on:
  push:
    branches:
      - 'master'
    tags:
      - "v*"
concurrency: push
jobs:
  push:
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build
        uses: ./.github/actions/build
      - name: Get uname
        id: uname
        run: |
          set -e
          echo "value=$(uname)" >> $GITHUB_OUTPUT
        shell: bash
        if: startsWith(github.ref, 'refs/tags/')
      - name: Get uname --machine
        id: uname_machine
        run: |
          set -e
          echo "value=$(uname --machine)" >> $GITHUB_OUTPUT
        shell: bash
        if: startsWith(github.ref, 'refs/tags/')
      - name: Define binary name
        id: binary_name
        run: |
          set -e
          echo "value=resonance.${{ steps.uname.outputs.value }}.${{ steps.uname_machine.outputs.value }}" >> $GITHUB_OUTPUT
        shell: bash
        if: startsWith(github.ref, 'refs/tags/')
      - name: Prepare binary
        run: |
          set -e
          mv resonance ${{ steps.binary_name.outputs.value }}
          gzip ${{ steps.binary_name.outputs.value }}
        shell: bash
        if: startsWith(github.ref, 'refs/tags/')
      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: ${{ steps.binary_name.outputs.value }}.gz